[project]
name = "reqforge-governance-mcp"
version = "0.1.0"
description = "ReqForge Governance MCP Server — migrated from SaucyClaw core/governance/"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "pyyaml>=6.0",
]

[project.scripts]
governance-mcp = "server:main"
